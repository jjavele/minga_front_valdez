import Carousel from "../componentes/Carousel"

export default function Index({nombre, version }) {
    console.log(nombre)
    console.log(version)
    return (
        
        <Carousel />
    )
}
